interface:
  display_name: "Image Gen"
  short_description: "Unified image prompts and batches for Windows/WSL"
  default_prompt: "Use $image-gen to plan, prompt, generate, edit, or batch image assets on Windows or WSL."
